import React from 'react'

function Login() {
    return (
        <div>
            <h1> Potluck Login </h1>
            <p> if user does not have an account, there can be a button on the bottom of the form that says "need an account?" which will lead to the register component </p>
        </div>
    )
}

export default Login
